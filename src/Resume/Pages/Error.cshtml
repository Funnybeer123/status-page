@page
@model ErrorModel

<div class="app">
    <header class="topbar">
        <div class="brand">
            <span class="mark" aria-hidden="true">EB</span>
            <p class="kicker">Resume</p>
        </div>
    </header>
    <header class="hero">
        <h1>Something went wrong</h1>
        <p class="headline">The page could not be rendered.</p>
    </header>
    <p class="source-banner" role="alert">
        <span class="banner-label">Error</span>
        Try the <a href="/">resume home page</a> again.
    </p>
</div>
